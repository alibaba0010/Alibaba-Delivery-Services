import React from "react";
import { sideBarItems } from "../../../app/configs/constants";

const Sidenav = () => {
  return <>{sideBarItems.map((i: SideBarItemsTypes, index: number) => {})}</>;
};

export default Sidenav;
